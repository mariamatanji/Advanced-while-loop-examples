# Simple-While-Loop
Integer inputNum is read from input. Write a loop that iterates while inputNum is not equal to 1. In each iteration of the loop:  Output "Still waiting for 1" and a newline. Read integer inputNum from input. 
